#include <iostream>
#include <stdio.h>

using namespace std;

int main(){

	double a,b,c,resultado;
	cin >> a;
	cin >> b;
	cin >> c;

	resultado = (a*2+b*3+c*5)/10;

	printf("MEDIA = %.1f\n", resultado);

}
