#include <iostream>
#include <stdio.h>

using namespace std;

int main(){

	int a, b, c, d, diferenca;

	cin >> a;
	cin >> b;
	cin >> c;
	cin >> d;

	diferenca = (a*b-c*d);

	printf("DIFERENCA = %d\n", diferenca);

}
